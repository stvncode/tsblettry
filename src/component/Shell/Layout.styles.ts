import { createStyles } from '@mantine/core'

export const useLayoutStyles = createStyles({
  flex: {
    height: '100%',
    padding: '0 20px',
  },
  container: {
    marginLeft: 'auto',
    marginRight: 'auto',
    marginTop: '5rem',
    maxWidth: '30rem',
    textAlign: 'center',
  },
})
